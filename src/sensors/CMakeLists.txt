include(${KOURETES_DIR}/cmakemodules/bootstrap.cmake)
use(NAOQI-PLUGINS-TOOLS)
add_library(sensors sensors.cpp)
target_link_libraries(sensors narukom)
use_lib(sensors ALCOMMON ALVALUE ALMATH LIBCORE TOOLS)
